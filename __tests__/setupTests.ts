import { App } from "obsidian";
//Before test starts, create a new folder and 3 blank files to test on.
beforeAll(() => {
    //Search for file-creating functions in Obsidian.
    
});
//After each test, clear the files of all data.
afterEach(() => {});
//After all tests, delete the folder and files.
afterAll(() => {});

export {};
