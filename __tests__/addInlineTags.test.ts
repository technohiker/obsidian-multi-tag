import MultiTagPlugin from "src/main";
import { App, Plugin, getAllTags } from "obsidian";

// it("Adds tags to all files in a folder", async () => {});

// it("Adds tags to all selected files", async () => {
// 	//Check that 3rd file was not tagged.
// });

// it("Adds tags to the bottom of a note", async () => {});

// it("Adds tags to the top of a note", async () => {});

// it("Trims out spaces from tags", async () => {});

it("Can run something from Obsidian.", async () => {
	// console.log(getAllTags())
});

export {};
